import { GameSearchFilterPipe } from './game-search-filter.pipe';

describe('GameSearchFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new GameSearchFilterPipe();
    expect(pipe).toBeTruthy();
  });
});

import { PlayerSearchFilterPipe } from './player-search-filter.pipe';

describe('PlayerSearchFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PlayerSearchFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
